[package]
name = "prewave-test-task-app"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.2", features = ["macros", "rt", "rt-multi-thread"] }
prewave-test-task-lib = { path = "../lib" }
